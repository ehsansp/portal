@page
@using PortalBuilder.Core.Convertors
@model PortalsBuilder.Web.Pages.Admin.Branch.IndexModel
@{
}
<div class="main-panel">
    <div class="content-wrapper">
        <div class="page-header">
            <h3 class="page-title">
                لیست شعب
            </h3>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="#">جداول</a></li>
                    <li class="breadcrumb-item active" aria-current="page">جدول داده</li>
                </ol>
            </nav>
        </div>
        <div class="card">
            <div class="card-body">
                <a href="/Admin/Branch/CreateBranch" class="btn btn-outline-success">جدید</a>
                <div class="row">
                    <div class="col-12">
                        <div class="table-responsive">
                            <table id="order-listing" class="table">
                                <thead>
                                    <tr>
                                        <th>ردیف</th>
                                        <th>ایجاد کننده</th>
                                        <th>تاریخ ایجاد</th>
                                        <th>عنوان</th>
                                        <th>نام مدیر</th>
                                        <th>شهر</th>
                                        <th>شماره تماس</th>
                                        <th>عملیات</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.ListBranch)
                                    {
                                        <tr>
                                            <td>@item.Id</td>
                                            <td>@item.CreatedBy</td>
                                            <td>@item.CreatedAt.Date.ToShamsi()</td>
                                            <td>@item.Title</td>
                                            <td>@item.ManagerName</td>
                                            <td>@item.City</td>
                                            <td>
                                                @item.Phone
                                            </td>
                                            <td>
                                                <a href="/Admin/Branch/EditBranch/@item.Id" class="btn btn-outline-primary">ویرایش</a>
                                                <button class="btn btn-outline-danger">حذف</button>
                                            </td>
                                        </tr>
                                    }
                                </tbody>

                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- content-wrapper ends -->
    <!-- partial:../../partials/_footer.html -->
    <footer class="footer">
        <div class="d-sm-flex justify-content-center justify-content-sm-between">
            <span class="text-muted text-center text-sm-left d-block d-sm-inline-block">
                کپی رایت © 2018 . تمام حقوق
                محفوظ است.
            </span>
        </div>
    </footer>
    <!-- partial -->
</div>