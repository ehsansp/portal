@page
@using PortalBuilder.Core.Convertors
@model PortalsBuilder.Web.Pages.Admin.Banner.IndexModel
@{
}
<div class="main-panel">
    <div class="content-wrapper">
        <div class="page-header">
            <h3 class="page-title">
                مدیریت بنر
            </h3>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="#">مدیریت بنر</a></li>
                    <li class="breadcrumb-item active" aria-current="page">مدیریت بنر</li>
                </ol>
            </nav>

        </div>
        <div class="card">
            <div class="card-body">
                <a href="/Admin/Banner/CreateBanner" class="btn btn-outline-success">جدید</a>
                <div class="row">
                    <div class="col-12">
                        <div class="table-responsive">
                            <table id="order-listing" class="table">
                                <thead>
                                    <tr>
                                        <th>ردیف</th>
                                        <th>ایجاد کننده</th>
                                        <th>تاریخ ایجاد</th>
                                        <th>عنوان</th>
                                        <th>تصویر</th>
                                        <th>تعداد کلیک</th>
                                        <th>وضعیت</th>
                                        <th>عملیات</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.ListBanner)
                                    {
                                    <tr>
                                        <td>@item.Id</td>
                                        <td>@item.CreatedBy</td>
                                        <td>@item.CreatedAt.Date.ToShamsi()</td>
                                        <td>@item.Title</td>
                                        <td>@item.Photo</td>
                                        <td>@item.ClickCount</td>
                                        <td>
                                            @item.IsActive
                                        </td>
                                        <td>
                                            <a href="/Admin/Banner/EditBanner/@item.Id" class="btn btn-outline-primary">ویرایش</a>
                                            <button class="btn btn-outline-danger">حذف</button>
                                        </td>
                                    </tr>
                                    }
                                    </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- content-wrapper ends -->
    <!-- partial:../../partials/_footer.html -->
    <footer class="footer">
        <div class="d-sm-flex justify-content-center justify-content-sm-between">
            <span class="text-muted text-center text-sm-left d-block d-sm-inline-block">
                کپی رایت © 2018 . تمام حقوق
                محفوظ است.
            </span>
        </div>
    </footer>
    <!-- partial -->
</div>